{
  "name": "get-to-post",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "test": "node --test",
    "build": "node --check api/builder.js && node --check lib/builder.js && node --check api/post.js && node --check lib/proxy.js && node --check lib/request-log.js",
    "db:setup": "node scripts/setup-db.mjs",
    "test:db": "node scripts/verify-db.mjs"
  },
  "dependencies": {
    "@neondatabase/serverless": "1.1.0",
    "ipaddr.js": "2.4.0"
  }
}
