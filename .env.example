# Runtime: insert-only Neon role. Never deploy the database owner connection.
REQUEST_LOG_DATABASE_URL=
# Optional converter authentication; unrelated to upstream request headers.
PROXY_API_KEY=

# Optional chunk API. Configure edge limits and the separate function-only role first.
CHUNK_UPLOADS_ENABLED=0
CHUNK_DATABASE_URL=
